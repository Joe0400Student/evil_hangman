#ifndef FILE_READ_H
#define FILE_READ_H

#include "vector.h"
#include "my_string.h"

vector vectorIterGenerator();

vector vectorSplitterSize(vector v);

int vcontains(vector v, int i);

vector chooseWordGroup(vector* v, int i);

#endif
